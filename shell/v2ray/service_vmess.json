{
  "log":{
    "loglevel": "${loglv}",
    "access": "/var/log/v2ray/access.log",
    "error": "/var/log/v2ray/error.log"
  },
  "inbound": {
    "port": ${port},
    "protocol": "vmess",
    "settings": {
      "clients": [
        {
          "id": "${uuid}", 
          "alterId": ${alterld}
        }
      ]${detour}
    }
  },
${dynamicPort}
  "outbound": {
    "protocol": "freedom",
    "settings": {}
  }
}